---
const pages = [
  {
    name: "Home",
    link: "/",
  },
  {
    name: "Experience",
    link: "/experience",
  },
  {
    name: "Works",
    link: "/works",
  },
];
---

<div
  class="flex fixed top-8 bg-primary shadow-primary-shiny text-primary shadow-primary text-white font-medium py-4 px-10 justify-between w-96 border-primary border rounded-full">
  {
    pages.map((page) => (
      <a
        href={page.link}
        class="cursor-pointer relative underline-animation">
        {page.name}
      </a>
    ))
  }
</div>

<style>
  .underline-animation::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -5px;
    width: 100%;
    height: 2px;
    background-color: #00adb4;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s ease;
  }

  .underline-animation:hover::after {
    transform: scaleX(1);
  }
</style>
